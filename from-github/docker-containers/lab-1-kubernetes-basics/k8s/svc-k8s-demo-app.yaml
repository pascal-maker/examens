apiVersion: v1
kind: Service
metadata:
  name: svc-k8s-demo-app
  namespace: howest-k8s-demo
spec:
  type: NodePort
  selector:
    app: k8s-demo
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30000
